import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "com.clashwithjpa.app",
    appName: "ClashWithJPA",
    webDir: "build",
};

export default config;
