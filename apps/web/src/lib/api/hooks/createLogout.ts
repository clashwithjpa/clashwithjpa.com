/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { LogoutMutationResponse, Logout500 } from "../models/Logout.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type { CreateMutationOptions, QueryClient } from "@tanstack/svelte-query";
import { logout } from "../clients/logout.ts";
import { createMutation } from "@tanstack/svelte-query";

export const logoutMutationKey = () => [{ url: "/logout" }] as const;

export type LogoutMutationKey = ReturnType<typeof logoutMutationKey>;

/**
 * @description [Public] Use this only for backend/scalar usage. For frontend, authClient should be used.
 * {@link /logout}
 */
export function createLogout<TContext>(
    options: {
        mutation?: CreateMutationOptions<LogoutMutationResponse, ResponseErrorConfig<Logout500>, void, TContext> & { client?: QueryClient };
        client?: Partial<RequestConfig> & { client?: Client };
    } = {},
) {
    const { mutation = {}, client: config = {} } = options ?? {};
    const { client: queryClient, ...mutationOptions } = mutation;
    const mutationKey = mutationOptions?.mutationKey ?? logoutMutationKey();

    return createMutation<LogoutMutationResponse, ResponseErrorConfig<Logout500>, void, TContext>(
        {
            mutationFn: async () => {
                return logout(config);
            },
            mutationKey,
            ...mutationOptions,
        },
        queryClient,
    );
}
