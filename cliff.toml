# git-cliff configuration — https://git-cliff.org/docs/configuration
#
# Drives the release PR in .github/workflows/release.yml: `--bumped-version`
# derives the next semver from conventional commits, `--bump` writes the section
# below into CHANGELOG.md. Commit links point at Gitea, not GitHub.

[changelog]
header = """
# Changelog

All notable changes to this project are documented in this file.

"""

body = """
{% if version %}\
## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | striptags | trim | upper_first }}

{% for commit in commits %}\
- {% if commit.breaking %}**breaking:** {% endif %}\
{% if commit.scope %}**{{ commit.scope }}:** {% endif %}\
{{ commit.message | upper_first }} \
([{{ commit.id | truncate(length=7, end="") }}](https://git.bymini.dev/clashwithjpa/clashwithjpa.com/commit/{{ commit.id }}))
{% endfor %}\
{% endfor %}
"""
footer = ""
trim = true

[bump]
initial_tag = "v2.0.0"

[git]
conventional_commits = true
filter_unconventional = true
protect_breaking_commits = true
split_commits = false
filter_commits = false
topo_order = false
sort_commits = "newest"
tag_pattern = "v[0-9]*"

commit_parsers = [
  { message = "^feat", group = "<!-- 0 -->Features" },
  { message = "^fix", group = "<!-- 1 -->Bug Fixes" },
  { message = "^perf", group = "<!-- 2 -->Performance" },
  { message = "^refactor", group = "<!-- 3 -->Refactor" },
  { message = "^revert", group = "<!-- 4 -->Reverts" },
  { message = "^docs", group = "<!-- 5 -->Documentation" },
  { message = "^test", group = "<!-- 6 -->Testing" },
  { message = "^build", group = "<!-- 7 -->Build" },
  { message = "^ci", group = "<!-- 8 -->CI" },
  { message = "^(chore\\(deps\\)|deps)", group = "<!-- 9 -->Dependencies" },
  { message = "^chore\\(release\\)", skip = true },
  { message = "^chore", skip = true },
  { message = "^style", skip = true },
]
