# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# Tech Stack
- Frontend: SvelteKit + Svelte 5. Confidence: 0.90
- Backend: Hono with better-auth. Confidence: 0.90

# Svelte
- Prefer Svelte's native `bind:value` over explicit `value` + `oninput` handlers. Confidence: 0.85

# Database Schema
- Use default values for optional fields instead of nullable columns when sensible (e.g., default 0 for numeric fields). Confidence: 0.80

