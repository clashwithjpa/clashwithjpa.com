/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type { GetRootQueryResponse } from "../models/GetRoot.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type { QueryKey, QueryClient, CreateBaseQueryOptions, CreateQueryResult } from "@tanstack/svelte-query";
import { createQuery, queryOptions } from "@tanstack/svelte-query";

export const getRootQueryKey = () => [{ url: "/" }] as const;

export type GetRootQueryKey = ReturnType<typeof getRootQueryKey>;

/**
 * @description [Public] Welcome route for the API. This route is used to verify that the API is up and running.
 * {@link /}
 */
export async function getRoot(config: Partial<RequestConfig> & { client?: Client } = {}) {
    const { client: request = fetch, ...requestConfig } = config;

    const res = await request<GetRootQueryResponse, ResponseErrorConfig<Error>, unknown>({ method: "GET", url: `/`, ...requestConfig });
    return res.data;
}

export function getRootQueryOptions(config: Partial<RequestConfig> & { client?: Client } = {}) {
    const queryKey = getRootQueryKey();
    return queryOptions<GetRootQueryResponse, ResponseErrorConfig<Error>, GetRootQueryResponse, typeof queryKey>({
        queryKey,
        queryFn: async ({ signal }) => {
            return getRoot({ ...config, signal: config.signal ?? signal });
        },
    });
}

/**
 * @description [Public] Welcome route for the API. This route is used to verify that the API is up and running.
 * {@link /}
 */
export function createGetRoot<TData = GetRootQueryResponse, TQueryData = GetRootQueryResponse, TQueryKey extends QueryKey = GetRootQueryKey>(
    options: {
        query?: Partial<CreateBaseQueryOptions<GetRootQueryResponse, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & {
            client?: QueryClient;
        };
        client?: Partial<RequestConfig> & { client?: Client };
    } = {},
) {
    const { query: queryConfig = {}, client: config = {} } = options ?? {};
    const { client: queryClient, ...resolvedOptions } = queryConfig;
    const queryKey = resolvedOptions?.queryKey ?? getRootQueryKey();

    const query = createQuery(
        {
            ...getRootQueryOptions(config),
            ...resolvedOptions,
            queryKey,
        } as unknown as CreateBaseQueryOptions,
        queryClient,
    ) as CreateQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

    query.queryKey = queryKey as TQueryKey;

    return query;
}
