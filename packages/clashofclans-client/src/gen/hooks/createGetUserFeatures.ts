/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { GetUserFeaturesQueryResponse, GetUserFeatures401, GetUserFeatures500 } from "../models/GetUserFeatures.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type { QueryKey, QueryClient, CreateBaseQueryOptions, CreateQueryResult } from "@tanstack/svelte-query";
import { getUserFeatures } from "../clients/getUserFeatures.ts";
import { createQuery, queryOptions } from "@tanstack/svelte-query";

export const getUserFeaturesQueryKey = () => [{ url: "/user/features" }] as const;

export type GetUserFeaturesQueryKey = ReturnType<typeof getUserFeaturesQueryKey>;

export function getUserFeaturesQueryOptions(config: Partial<RequestConfig> & { client?: Client } = {}) {
    const queryKey = getUserFeaturesQueryKey();
    return queryOptions<
        GetUserFeaturesQueryResponse,
        ResponseErrorConfig<GetUserFeatures401 | GetUserFeatures500>,
        GetUserFeaturesQueryResponse,
        typeof queryKey
    >({
        queryKey,
        queryFn: async ({ signal }) => {
            return getUserFeatures({ ...config, signal: config.signal ?? signal });
        },
    });
}

/**
 * @description [Authenticated] Fetches the site feature flags relevant to the current user.
 * {@link /user/features}
 */
export function createGetUserFeatures<
    TData = GetUserFeaturesQueryResponse,
    TQueryData = GetUserFeaturesQueryResponse,
    TQueryKey extends QueryKey = GetUserFeaturesQueryKey,
>(
    options: {
        query?: Partial<
            CreateBaseQueryOptions<
                GetUserFeaturesQueryResponse,
                ResponseErrorConfig<GetUserFeatures401 | GetUserFeatures500>,
                TData,
                TQueryData,
                TQueryKey
            >
        > & { client?: QueryClient };
        client?: Partial<RequestConfig> & { client?: Client };
    } = {},
) {
    const { query: queryConfig = {}, client: config = {} } = options ?? {};
    const { client: queryClient, ...resolvedOptions } = queryConfig;
    const queryKey = resolvedOptions?.queryKey ?? getUserFeaturesQueryKey();

    const query = createQuery(
        {
            ...getUserFeaturesQueryOptions(config),
            ...resolvedOptions,
            queryKey,
        } as unknown as CreateBaseQueryOptions,
        queryClient,
    ) as CreateQueryResult<TData, ResponseErrorConfig<GetUserFeatures401 | GetUserFeatures500>> & { queryKey: TQueryKey };

    query.queryKey = queryKey as TQueryKey;

    return query;
}
