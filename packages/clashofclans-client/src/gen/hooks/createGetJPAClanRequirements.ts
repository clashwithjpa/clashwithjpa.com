/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { GetJPAClanRequirementsQueryResponse, GetJPAClanRequirements500 } from "../models/GetJPAClanRequirements.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type { QueryKey, QueryClient, CreateBaseQueryOptions, CreateQueryResult } from "@tanstack/svelte-query";
import { getJPAClanRequirements } from "../clients/getJPAClanRequirements.ts";
import { createQuery, queryOptions } from "@tanstack/svelte-query";

export const getJPAClanRequirementsQueryKey = () => [{ url: "/coc/jpa/clans/requirements" }] as const;

export type GetJPAClanRequirementsQueryKey = ReturnType<typeof getJPAClanRequirementsQueryKey>;

export function getJPAClanRequirementsQueryOptions(config: Partial<RequestConfig> & { client?: Client } = {}) {
    const queryKey = getJPAClanRequirementsQueryKey();
    return queryOptions<
        GetJPAClanRequirementsQueryResponse,
        ResponseErrorConfig<GetJPAClanRequirements500>,
        GetJPAClanRequirementsQueryResponse,
        typeof queryKey
    >({
        queryKey,
        queryFn: async ({ signal }) => {
            return getJPAClanRequirements({ ...config, signal: config.signal ?? signal });
        },
    });
}

/**
 * @description [Public] Fetches all JPA clans and their requirements.
 * {@link /coc/jpa/clans/requirements}
 */
export function createGetJPAClanRequirements<
    TData = GetJPAClanRequirementsQueryResponse,
    TQueryData = GetJPAClanRequirementsQueryResponse,
    TQueryKey extends QueryKey = GetJPAClanRequirementsQueryKey,
>(
    options: {
        query?: Partial<
            CreateBaseQueryOptions<GetJPAClanRequirementsQueryResponse, ResponseErrorConfig<GetJPAClanRequirements500>, TData, TQueryData, TQueryKey>
        > & { client?: QueryClient };
        client?: Partial<RequestConfig> & { client?: Client };
    } = {},
) {
    const { query: queryConfig = {}, client: config = {} } = options ?? {};
    const { client: queryClient, ...resolvedOptions } = queryConfig;
    const queryKey = resolvedOptions?.queryKey ?? getJPAClanRequirementsQueryKey();

    const query = createQuery(
        {
            ...getJPAClanRequirementsQueryOptions(config),
            ...resolvedOptions,
            queryKey,
        } as unknown as CreateBaseQueryOptions,
        queryClient,
    ) as CreateQueryResult<TData, ResponseErrorConfig<GetJPAClanRequirements500>> & { queryKey: TQueryKey };

    query.queryKey = queryKey as TQueryKey;

    return query;
}
