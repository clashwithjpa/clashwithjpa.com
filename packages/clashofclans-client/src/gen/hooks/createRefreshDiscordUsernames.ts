/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
    RefreshDiscordUsernamesMutationResponse,
    RefreshDiscordUsernames401,
    RefreshDiscordUsernames500,
    RefreshDiscordUsernames503,
} from "../models/RefreshDiscordUsernames.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type { CreateMutationOptions, QueryClient } from "@tanstack/svelte-query";
import { refreshDiscordUsernames } from "../clients/refreshDiscordUsernames.ts";
import { createMutation } from "@tanstack/svelte-query";

export const refreshDiscordUsernamesMutationKey = () => [{ url: "/admin/refresh-discord-usernames" }] as const;

export type RefreshDiscordUsernamesMutationKey = ReturnType<typeof refreshDiscordUsernamesMutationKey>;

/**
 * @description [Superadmin/root] Re-fetches Discord usernames from the guild member list and updates stored values.
 * {@link /admin/refresh-discord-usernames}
 */
export function createRefreshDiscordUsernames<TContext>(
    options: {
        mutation?: CreateMutationOptions<
            RefreshDiscordUsernamesMutationResponse,
            ResponseErrorConfig<RefreshDiscordUsernames401 | RefreshDiscordUsernames500 | RefreshDiscordUsernames503>,
            void,
            TContext
        > & { client?: QueryClient };
        client?: Partial<RequestConfig> & { client?: Client };
    } = {},
) {
    const { mutation = {}, client: config = {} } = options ?? {};
    const { client: queryClient, ...mutationOptions } = mutation;
    const mutationKey = mutationOptions?.mutationKey ?? refreshDiscordUsernamesMutationKey();

    return createMutation<
        RefreshDiscordUsernamesMutationResponse,
        ResponseErrorConfig<RefreshDiscordUsernames401 | RefreshDiscordUsernames500 | RefreshDiscordUsernames503>,
        void,
        TContext
    >(
        {
            mutationFn: async () => {
                return refreshDiscordUsernames(config);
            },
            mutationKey,
            ...mutationOptions,
        },
        queryClient,
    );
}
