/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { GetBonusLedgerQueryResponse, GetBonusLedger401, GetBonusLedger500 } from "../models/GetBonusLedger.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type { QueryKey, QueryClient, CreateBaseQueryOptions, CreateQueryResult } from "@tanstack/svelte-query";
import { getBonusLedger } from "../clients/getBonusLedger.ts";
import { createQuery, queryOptions } from "@tanstack/svelte-query";

export const getBonusLedgerQueryKey = () => [{ url: "/admin/bonus-ledger" }] as const;

export type GetBonusLedgerQueryKey = ReturnType<typeof getBonusLedgerQueryKey>;

export function getBonusLedgerQueryOptions(config: Partial<RequestConfig> & { client?: Client } = {}) {
    const queryKey = getBonusLedgerQueryKey();
    return queryOptions<
        GetBonusLedgerQueryResponse,
        ResponseErrorConfig<GetBonusLedger401 | GetBonusLedger500>,
        GetBonusLedgerQueryResponse,
        typeof queryKey
    >({
        queryKey,
        queryFn: async ({ signal }) => {
            return getBonusLedger({ ...config, signal: config.signal ?? signal });
        },
    });
}

/**
 * @description [Manager] Lists every awarded bonus (one row per user per season).
 * {@link /admin/bonus-ledger}
 */
export function createGetBonusLedger<
    TData = GetBonusLedgerQueryResponse,
    TQueryData = GetBonusLedgerQueryResponse,
    TQueryKey extends QueryKey = GetBonusLedgerQueryKey,
>(
    options: {
        query?: Partial<
            CreateBaseQueryOptions<
                GetBonusLedgerQueryResponse,
                ResponseErrorConfig<GetBonusLedger401 | GetBonusLedger500>,
                TData,
                TQueryData,
                TQueryKey
            >
        > & { client?: QueryClient };
        client?: Partial<RequestConfig> & { client?: Client };
    } = {},
) {
    const { query: queryConfig = {}, client: config = {} } = options ?? {};
    const { client: queryClient, ...resolvedOptions } = queryConfig;
    const queryKey = resolvedOptions?.queryKey ?? getBonusLedgerQueryKey();

    const query = createQuery(
        {
            ...getBonusLedgerQueryOptions(config),
            ...resolvedOptions,
            queryKey,
        } as unknown as CreateBaseQueryOptions,
        queryClient,
    ) as CreateQueryResult<TData, ResponseErrorConfig<GetBonusLedger401 | GetBonusLedger500>> & { queryKey: TQueryKey };

    query.queryKey = queryKey as TQueryKey;

    return query;
}
