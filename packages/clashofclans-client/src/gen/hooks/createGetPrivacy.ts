/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type { CreateBaseQueryOptions, CreateQueryResult, QueryClient, QueryKey } from "@tanstack/svelte-query";
import { createQuery, queryOptions } from "@tanstack/svelte-query";
import { getPrivacy } from "../clients/getPrivacy.ts";
import type { GetPrivacy500, GetPrivacyQueryResponse } from "../models/GetPrivacy.ts";

export const getPrivacyQueryKey = () => [{ url: "/privacy" }] as const;

export type GetPrivacyQueryKey = ReturnType<typeof getPrivacyQueryKey>;

export function getPrivacyQueryOptions(config: Partial<RequestConfig> & { client?: Client } = {}) {
    const queryKey = getPrivacyQueryKey();
    return queryOptions<GetPrivacyQueryResponse, ResponseErrorConfig<GetPrivacy500>, GetPrivacyQueryResponse, typeof queryKey>({
        queryKey,
        queryFn: async ({ signal }) => {
            return getPrivacy({ ...config, signal: config.signal ?? signal });
        },
    });
}

/**
 * @description Fetches the current privacy policy content.
 * {@link /privacy}
 */
export function createGetPrivacy<
    TData = GetPrivacyQueryResponse,
    TQueryData = GetPrivacyQueryResponse,
    TQueryKey extends QueryKey = GetPrivacyQueryKey,
>(
    options: {
        query?: Partial<CreateBaseQueryOptions<GetPrivacyQueryResponse, ResponseErrorConfig<GetPrivacy500>, TData, TQueryData, TQueryKey>> & {
            client?: QueryClient;
        };
        client?: Partial<RequestConfig> & { client?: Client };
    } = {},
) {
    const { query: queryConfig = {}, client: config = {} } = options ?? {};
    const { client: queryClient, ...resolvedOptions } = queryConfig;
    const queryKey = resolvedOptions?.queryKey ?? getPrivacyQueryKey();

    const query = createQuery(
        {
            ...getPrivacyQueryOptions(config),
            ...resolvedOptions,
            queryKey,
        } as unknown as CreateBaseQueryOptions,
        queryClient,
    ) as CreateQueryResult<TData, ResponseErrorConfig<GetPrivacy500>> & { queryKey: TQueryKey };

    query.queryKey = queryKey as TQueryKey;

    return query;
}
