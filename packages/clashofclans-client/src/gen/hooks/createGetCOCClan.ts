/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type { GetCOCClanQueryResponse, GetCOCClanPathParams, GetCOCClan500 } from "../models/GetCOCClan.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type { QueryKey, QueryClient, CreateBaseQueryOptions, CreateQueryResult } from "@tanstack/svelte-query";
import { createQuery, queryOptions } from "@tanstack/svelte-query";

export const getCOCClanQueryKey = (tag: GetCOCClanPathParams["tag"]) => [{ url: "/coc/clan/:tag", params: { tag: tag } }] as const;

export type GetCOCClanQueryKey = ReturnType<typeof getCOCClanQueryKey>;

/**
 * @description [Public] Fetches a Clash of Clans clan's data by its tag. The tag must start with #.
 * {@link /coc/clan/:tag}
 */
export async function getCOCClan(tag: GetCOCClanPathParams["tag"], config: Partial<RequestConfig> & { client?: Client } = {}) {
    const { client: request = fetch, ...requestConfig } = config;

    const res = await request<GetCOCClanQueryResponse, ResponseErrorConfig<GetCOCClan500>, unknown>({
        method: "GET",
        url: `/coc/clan/${tag}`,
        ...requestConfig,
    });
    return res.data;
}

export function getCOCClanQueryOptions(tag: GetCOCClanPathParams["tag"], config: Partial<RequestConfig> & { client?: Client } = {}) {
    const queryKey = getCOCClanQueryKey(tag);
    return queryOptions<GetCOCClanQueryResponse, ResponseErrorConfig<GetCOCClan500>, GetCOCClanQueryResponse, typeof queryKey>({
        enabled: !!tag,
        queryKey,
        queryFn: async ({ signal }) => {
            return getCOCClan(tag, { ...config, signal: config.signal ?? signal });
        },
    });
}

/**
 * @description [Public] Fetches a Clash of Clans clan's data by its tag. The tag must start with #.
 * {@link /coc/clan/:tag}
 */
export function createGetCOCClan<
    TData = GetCOCClanQueryResponse,
    TQueryData = GetCOCClanQueryResponse,
    TQueryKey extends QueryKey = GetCOCClanQueryKey,
>(
    tag: GetCOCClanPathParams["tag"],
    options: {
        query?: Partial<CreateBaseQueryOptions<GetCOCClanQueryResponse, ResponseErrorConfig<GetCOCClan500>, TData, TQueryData, TQueryKey>> & {
            client?: QueryClient;
        };
        client?: Partial<RequestConfig> & { client?: Client };
    } = {},
) {
    const { query: queryConfig = {}, client: config = {} } = options ?? {};
    const { client: queryClient, ...resolvedOptions } = queryConfig;
    const queryKey = resolvedOptions?.queryKey ?? getCOCClanQueryKey(tag);

    const query = createQuery(
        {
            ...getCOCClanQueryOptions(tag, config),
            ...resolvedOptions,
            queryKey,
        } as unknown as CreateBaseQueryOptions,
        queryClient,
    ) as CreateQueryResult<TData, ResponseErrorConfig<GetCOCClan500>> & { queryKey: TQueryKey };

    query.queryKey = queryKey as TQueryKey;

    return query;
}
