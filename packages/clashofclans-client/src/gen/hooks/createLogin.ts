/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type { LoginQueryResponse } from "../models/Login.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type { QueryKey, QueryClient, CreateBaseQueryOptions, CreateQueryResult } from "@tanstack/svelte-query";
import { createQuery, queryOptions } from "@tanstack/svelte-query";

export const loginQueryKey = () => [{ url: "/login" }] as const;

export type LoginQueryKey = ReturnType<typeof loginQueryKey>;

/**
 * @description [Public] Use this only for backend/scalar usage. For frontend, authClient should be used.
 * {@link /login}
 */
export async function login(config: Partial<RequestConfig> & { client?: Client } = {}) {
    const { client: request = fetch, ...requestConfig } = config;

    const res = await request<LoginQueryResponse, ResponseErrorConfig<Error>, unknown>({ method: "GET", url: `/login`, ...requestConfig });
    return res.data;
}

export function loginQueryOptions(config: Partial<RequestConfig> & { client?: Client } = {}) {
    const queryKey = loginQueryKey();
    return queryOptions<LoginQueryResponse, ResponseErrorConfig<Error>, LoginQueryResponse, typeof queryKey>({
        queryKey,
        queryFn: async ({ signal }) => {
            return login({ ...config, signal: config.signal ?? signal });
        },
    });
}

/**
 * @description [Public] Use this only for backend/scalar usage. For frontend, authClient should be used.
 * {@link /login}
 */
export function createLogin<TData = LoginQueryResponse, TQueryData = LoginQueryResponse, TQueryKey extends QueryKey = LoginQueryKey>(
    options: {
        query?: Partial<CreateBaseQueryOptions<LoginQueryResponse, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & {
            client?: QueryClient;
        };
        client?: Partial<RequestConfig> & { client?: Client };
    } = {},
) {
    const { query: queryConfig = {}, client: config = {} } = options ?? {};
    const { client: queryClient, ...resolvedOptions } = queryConfig;
    const queryKey = resolvedOptions?.queryKey ?? loginQueryKey();

    const query = createQuery(
        {
            ...loginQueryOptions(config),
            ...resolvedOptions,
            queryKey,
        } as unknown as CreateBaseQueryOptions,
        queryClient,
    ) as CreateQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

    query.queryKey = queryKey as TQueryKey;

    return query;
}
