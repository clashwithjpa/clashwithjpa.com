/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { GetCwlStatsQueryResponse, GetCwlStatsQueryParams, GetCwlStats401, GetCwlStats500 } from "../models/GetCwlStats.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type { QueryKey, QueryClient, CreateBaseQueryOptions, CreateQueryResult } from "@tanstack/svelte-query";
import { getCwlStats } from "../clients/getCwlStats.ts";
import { createQuery, queryOptions } from "@tanstack/svelte-query";

export const getCwlStatsQueryKey = (params?: GetCwlStatsQueryParams) => [{ url: "/admin/cwl-stats" }, ...(params ? [params] : [])] as const;

export type GetCwlStatsQueryKey = ReturnType<typeof getCwlStatsQueryKey>;

export function getCwlStatsQueryOptions(params?: GetCwlStatsQueryParams, config: Partial<RequestConfig> & { client?: Client } = {}) {
    const queryKey = getCwlStatsQueryKey(params);
    return queryOptions<GetCwlStatsQueryResponse, ResponseErrorConfig<GetCwlStats401 | GetCwlStats500>, GetCwlStatsQueryResponse, typeof queryKey>({
        queryKey,
        queryFn: async ({ signal }) => {
            return getCwlStats(params, { ...config, signal: config.signal ?? signal });
        },
    });
}

/**
 * @description [Manager] Live-fetches the current CWL from the CoC API for the season's assigned clans; returns per-player attacks used (of 7) and stars with a per-attack breakdown.
 * {@link /admin/cwl-stats}
 */
export function createGetCwlStats<
    TData = GetCwlStatsQueryResponse,
    TQueryData = GetCwlStatsQueryResponse,
    TQueryKey extends QueryKey = GetCwlStatsQueryKey,
>(
    params?: GetCwlStatsQueryParams,
    options: {
        query?: Partial<
            CreateBaseQueryOptions<GetCwlStatsQueryResponse, ResponseErrorConfig<GetCwlStats401 | GetCwlStats500>, TData, TQueryData, TQueryKey>
        > & { client?: QueryClient };
        client?: Partial<RequestConfig> & { client?: Client };
    } = {},
) {
    const { query: queryConfig = {}, client: config = {} } = options ?? {};
    const { client: queryClient, ...resolvedOptions } = queryConfig;
    const queryKey = resolvedOptions?.queryKey ?? getCwlStatsQueryKey(params);

    const query = createQuery(
        {
            ...getCwlStatsQueryOptions(params, config),
            ...resolvedOptions,
            queryKey,
        } as unknown as CreateBaseQueryOptions,
        queryClient,
    ) as CreateQueryResult<TData, ResponseErrorConfig<GetCwlStats401 | GetCwlStats500>> & { queryKey: TQueryKey };

    query.queryKey = queryKey as TQueryKey;

    return query;
}
