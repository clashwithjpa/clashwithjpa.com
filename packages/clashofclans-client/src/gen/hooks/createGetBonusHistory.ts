/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { GetBonusHistoryQueryResponse, GetBonusHistory401, GetBonusHistory500 } from "../models/GetBonusHistory.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type { QueryKey, QueryClient, CreateBaseQueryOptions, CreateQueryResult } from "@tanstack/svelte-query";
import { getBonusHistory } from "../clients/getBonusHistory.ts";
import { createQuery, queryOptions } from "@tanstack/svelte-query";

export const getBonusHistoryQueryKey = () => [{ url: "/admin/bonus-history" }] as const;

export type GetBonusHistoryQueryKey = ReturnType<typeof getBonusHistoryQueryKey>;

export function getBonusHistoryQueryOptions(config: Partial<RequestConfig> & { client?: Client } = {}) {
    const queryKey = getBonusHistoryQueryKey();
    return queryOptions<
        GetBonusHistoryQueryResponse,
        ResponseErrorConfig<GetBonusHistory401 | GetBonusHistory500>,
        GetBonusHistoryQueryResponse,
        typeof queryKey
    >({
        queryKey,
        queryFn: async ({ signal }) => {
            return getBonusHistory({ ...config, signal: config.signal ?? signal });
        },
    });
}

/**
 * @description [Manager] Lists every per-account bonus row (ticked months) for the bonus table.
 * {@link /admin/bonus-history}
 */
export function createGetBonusHistory<
    TData = GetBonusHistoryQueryResponse,
    TQueryData = GetBonusHistoryQueryResponse,
    TQueryKey extends QueryKey = GetBonusHistoryQueryKey,
>(
    options: {
        query?: Partial<
            CreateBaseQueryOptions<
                GetBonusHistoryQueryResponse,
                ResponseErrorConfig<GetBonusHistory401 | GetBonusHistory500>,
                TData,
                TQueryData,
                TQueryKey
            >
        > & { client?: QueryClient };
        client?: Partial<RequestConfig> & { client?: Client };
    } = {},
) {
    const { query: queryConfig = {}, client: config = {} } = options ?? {};
    const { client: queryClient, ...resolvedOptions } = queryConfig;
    const queryKey = resolvedOptions?.queryKey ?? getBonusHistoryQueryKey();

    const query = createQuery(
        {
            ...getBonusHistoryQueryOptions(config),
            ...resolvedOptions,
            queryKey,
        } as unknown as CreateBaseQueryOptions,
        queryClient,
    ) as CreateQueryResult<TData, ResponseErrorConfig<GetBonusHistory401 | GetBonusHistory500>> & { queryKey: TQueryKey };

    query.queryKey = queryKey as TQueryKey;

    return query;
}
