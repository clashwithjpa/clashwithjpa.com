/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/fetch";
import type { GetBonusHistoryQueryResponse, GetBonusHistory401, GetBonusHistory500 } from "../models/GetBonusHistory.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/fetch";

function getGetBonusHistoryUrl() {
    const res = { method: "GET", url: `/admin/bonus-history` as const };
    return res;
}

/**
 * @description [Manager] Lists every per-account bonus row (ticked months) for the bonus table.
 * {@link /admin/bonus-history}
 */
export async function getBonusHistory(config: Partial<RequestConfig> & { client?: Client } = {}) {
    const { client: request = fetch, ...requestConfig } = config;

    const res = await request<GetBonusHistoryQueryResponse, ResponseErrorConfig<GetBonusHistory401 | GetBonusHistory500>, unknown>({
        method: "GET",
        url: getGetBonusHistoryUrl().url.toString(),
        ...requestConfig,
    });
    return res.data;
}
