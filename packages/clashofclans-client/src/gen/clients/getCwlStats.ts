/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/fetch";
import type { GetCwlStatsQueryResponse, GetCwlStatsQueryParams, GetCwlStats401, GetCwlStats500 } from "../models/GetCwlStats.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/fetch";

function getGetCwlStatsUrl() {
    const res = { method: "GET", url: `/admin/cwl-stats` as const };
    return res;
}

/**
 * @description [Manager] Live-fetches the current CWL from the CoC API for the season's assigned clans; returns per-player attacks used (of 7) and stars with a per-attack breakdown.
 * {@link /admin/cwl-stats}
 */
export async function getCwlStats(params?: GetCwlStatsQueryParams, config: Partial<RequestConfig> & { client?: Client } = {}) {
    const { client: request = fetch, ...requestConfig } = config;

    const res = await request<GetCwlStatsQueryResponse, ResponseErrorConfig<GetCwlStats401 | GetCwlStats500>, unknown>({
        method: "GET",
        url: getGetCwlStatsUrl().url.toString(),
        params,
        ...requestConfig,
    });
    return res.data;
}
